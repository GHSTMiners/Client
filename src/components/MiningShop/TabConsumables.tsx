import React, { FC, Fragment } from "react";
import styles from "./styles.module.css"

const TabConsumables: FC<{}> = () => {
  return (
    <div className={styles.contentContainer}>
      Add list of consumables
    </div>
  );
};
export default TabConsumables;
